1.list
    list.pop([index = -1]) 移除列表中的一个元素（默认最后一个元素），并且返回该元素的值;即可以删除倒数第几个数

2.set
    set.add的顺序和保存在set里面的顺序并不一致(list和dic的顺序是一致的)

3.dic
    循环key与value：for key, value in dic.items() / for key in dic  / for value in dic.values()
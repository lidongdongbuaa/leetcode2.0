1.求平方: a = 2**3 = 8

2.'==': 对两个值进行比较
  'is': 比较两个对象(地址, id值),而由于None的地址是一致的，故None/True可以用is/==判断
  == 与 is 什么时候相同：
    数字类型不完全相同
    字符串类型不完全相同。这个和解释器实现有关。
    故认为 当变量是数字、字符串、元组，列表，字典时，is和==都不相同， 不能互换使用

3. a = [] -> (not a) is True / (a is None/False) is False
    故老老实实用 a == []

1. for的条件主要是长度，即次数的循环；而while的条件更注重 判断条件的循环，即A!=B 这类软条件
2. 循环归根到底在规定范围内的循环，
    对于for，是设定的循环次数；
    对于while，是判断条件的从头到尾，比如在链表的从头到尾循环中就能解决问题
3. 返回条件/返回值：问题可以分成几类，就有几个返回条件/返回值
4. for 语法：
    1. i in range(5,5): 语句没问题，返回为空
    2. enumerate():
        nums= [2,3,4,5]
        for i, n in enumerate(nums):
            print(i,n)
        0,2/1,3/.....
